using ScriptCoreLib;
using java.net;

namespace java.security
{
    [Script(IsNative=true)]
    public class CodeSource
    {
        public URL getLocation()
        {
            return default(URL);
        }
    }
}
